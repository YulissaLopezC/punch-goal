// Copia este archivo como firebase-config.js y llena tus credenciales para desarrollo local.
// firebase-config.js está en .gitignore — nunca se sube al repo.
export const firebaseConfig = {
  apiKey:            "TU_API_KEY",
  authDomain:        "TU_AUTH_DOMAIN",
  projectId:         "TU_PROJECT_ID",
  storageBucket:     "TU_STORAGE_BUCKET",
  messagingSenderId: "TU_MESSAGING_SENDER_ID",
  appId:             "TU_APP_ID"
};
